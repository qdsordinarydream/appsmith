export const getLicenseKey = () => "";

// 修改源标识，不再跳转到pricing页面
export const pricingPageUrlSource = "";

// 确保不显示任何升级菜单项
export const ShowUpgradeMenuItem = () => null;

export const Banner = () => null;
